import { MONTH_VIEW_LAYOUT } from '../config/month-view.config';

/**
 * Month View Renderer
 * 
 * Provides layout calculations for the monthly calendar grid.
 */
export class MonthViewRenderer {
  private readonly DAY_WIDTH_PERCENT = MONTH_VIEW_LAYOUT.DAY_WIDTH_PERCENT;

  // Renders only grid related logic if needed, but for now we follow the request 
  // to remove all event rendering and calculation.
}


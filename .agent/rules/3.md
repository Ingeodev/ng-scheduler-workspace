---
trigger: always_on
---

Dont use semicolon in the end line.

Don't code magic values. Olways declare constants to avoid this behavior.

Inside to ng-scheduler use global styles declared into styles folder for the css rules like a hierarchy and create a new css variable to overwrite the value in a specific components.

To create new components, analyze if template will have more than 3 lines. If this is true, create separate html template.

Based on angular scaffolder style, always decide if a new logic must be inside to core or shared instead.